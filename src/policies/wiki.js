const ApplicationPolicy = require("./application");

module.exports = class WikiPolicy extends ApplicationPolicy {
  /* if there are no methods here, the caller of this class will go looking for methods in the parent class, ApplicationPolicy */
}
